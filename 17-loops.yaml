- name: Loops demo to install multiple packages
  hosts: web
  become: true
  tasks:
  - name: print names
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx
    - httpd