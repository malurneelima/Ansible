- name: Loops demo to install multiple packages
  hosts: local
  connection: local
  become: true
  tasks:
  - name: print names
    ansible.builtin.package:
      name: "{{ item }}""
      state: present
    loop:
    - mysql
    - nginx
    - httpd